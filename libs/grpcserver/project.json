{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "libs-grpcserver",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": { "command": "go build ./...", "cwd": "libs/grpcserver" }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": { "command": "go test ./...", "cwd": "libs/grpcserver" }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "golangci-lint run --allow-parallel-runners",
        "cwd": "libs/grpcserver"
      }
    }
  }
}
