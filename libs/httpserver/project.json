{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "libs-httpserver",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": { "command": "go build ./...", "cwd": "libs/httpserver" }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": { "command": "go test ./...", "cwd": "libs/httpserver" }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "golangci-lint run --allow-parallel-runners",
        "cwd": "libs/httpserver"
      }
    }
  }
}
